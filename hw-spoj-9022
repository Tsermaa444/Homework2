#include <iostream>

using namespace std;

int main(){

    int n, a;
    cout << " a=";
    cin >> a;
    
    cout << "n=";
    cin >> n;
    
    int sum = 0;

    
    string s = "IOI";
    
    for (int i = 1; i <= n; i ++)
    sum = sum + a;
    
        cout << sum << "\n";
    
    
    
    
    return 0;
}
